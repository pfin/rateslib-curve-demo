# Try both libraries to see which works on Vercel
# Rateslib - Fixed income library with Rust extensions
rateslib==2.0.0; platform_machine == "x86_64" and platform_system == "Linux"

# QuantLib - Fixed income library with C++ extensions
QuantLib==1.34; platform_machine == "x86_64" and platform_system == "Linux"